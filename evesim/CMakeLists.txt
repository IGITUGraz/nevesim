FILE(GLOB SOURCES RELATIVE "${PROJECT_SOURCE_DIR}/evesim" "*.cpp" )

ADD_LIBRARY( evesim SHARED ${SOURCES} )

INSTALL(TARGETS evesim 
		DESTINATION "lib"
		PERMISSIONS OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ		 
		CONFIGURATIONS Debug Release RELWITHDEBINFO)
		